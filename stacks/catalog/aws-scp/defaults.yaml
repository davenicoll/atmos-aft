# Stack-attached SCP. Target resolves to the OU or account via Organizations
# data sources in the component. Leaf stacks supply `statements` + `target_id`.
components:
  terraform:
    aws-scp:
      metadata:
        component: aws-scp
      vars: {}
