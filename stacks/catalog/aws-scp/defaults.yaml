# Stack-attached SCP. Leaf stacks supply `statements` + `target_id`; target
# resolves to the OU or account via Organizations data sources in the component.
components:
  terraform:
    aws-scp:
      metadata:
        component: aws-scp
      vars: {}
