# Global (org-scope) resources. AWS region is pinned to the CT home region
# but `environment` is `gbl` so stack names don't collide with regional instances.
vars:
  region: us-east-1
  environment: gbl
