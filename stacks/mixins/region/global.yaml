# Global (org-scope) resources. `environment` is `gbl` so stack names don't
# collide with regional instances. The AWS home region is inherited from the
# org-level `_defaults.yaml` (`vars.region`).
vars:
  environment: gbl
